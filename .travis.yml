dist: focal
addons:
  apt:
    packages:
    - binaryen
language: rust
rust:
  - nightly
jobs:
  fast_finish: true
cache: cargo
before_install: 
  - rustup component add rust-src
install:
  - cargo install cargo-contract --force
script:
  - cargo contract build
  - cargo test